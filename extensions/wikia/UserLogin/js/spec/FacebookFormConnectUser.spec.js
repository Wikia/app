describe('Form for connecting facebook to wikia accounts', function () {
	'use strict';

	var $el, options;

	beforeEach(function () {
		$el = $('<div><form><input type="text"></form></div>');
		options = {skipFocus: true};
	});

	it ('Expects an element and some options', function () {

	});

	it ('Inherits required methods from the base class', function () {

	});

	it ('Sets some properties on init', function () {

	});
});
