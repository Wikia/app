(function($) {

$.fn.createDropdown = function(options) {
	options = $.extend(true, {}, $.fn.createDropdown.options, options);

	return this.each(function() {
		// todo
	});
};

$.fn.createDropdown.options = {};

})(jQuery);