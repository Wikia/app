// Generated by CoffeeScript 1.6.1
(function() {

	window.api_key = 'a2a73e7b926c924fad7001ca3111acd55af2ffabf50eb4ae5';

	window.discoveryUrl = "http://api.wordnik.com/v4/resources.json";

	describe('SwaggerUi', function() {
		describe('constructor', function() {
			beforeEach(function() {
				window.ui = new SwaggerUi({
					api_key: window.api_key,
					discoveryUrl: window.discoveryUrl
				});
				return waitsFor(function() {
					return ui.ready;
				});
			});
			it("sets a `ready` property when the API is ready", function() {
				return runs(function() {
					return expect(ui.ready).toBe(true);
				});
			});
			return it("has access to the precompiled Handlebars template", function() {
				return runs(function() {
					return expect(Handlebars.templates['template.html']).toBeDefined();
				});
			});
		});
		return describe('DOM container', function() {
			afterEach(function() {
				return $("#swagger_ui").remove();
			});
			it("renders to default DOM container if it exists", function() {
				window.ui = new SwaggerUi;
				waitsFor(function() {
					return ui.ready;
				});
				return runs(function() {
					return expect($("#swagger_ui").length).toBe(1);
				});
			});
			it("creates default DOM container if it doesn't exist", function() {
				expect($("#swagger_ui").length).toBe(0);
				window.ui = new SwaggerUi;
				waitsFor(function() {
					return ui.ready;
				});
				return runs(function() {
					return expect($("#swagger_ui").length).toBe(1);
				});
			});
			return it("allows an alternate DOM container to be specified when initialized", function() {
				expect($("#swagger_ui").length).toBe(0);
				window.ui = new SwaggerUi({
					dom_id: 'zhwagger'
				});
				waitsFor(function() {
					return ui.ready;
				});
				return runs(function() {
					return expect($("#zhwagger").length).toBe(1);
				});
			});
		});
	});

}).call(this);
