<section class="wikia-interactive-maps {{^hasMaps}}no-maps{{/hasMaps}}">
	<nav class="maps-controls">
		{{{searchInput}}}
		{{#hasMaps}}
		<button id="createMap">{{messages.create-a-map}}</button>
		<select id="orderMapList">
			{{#sortingOptions}}
			<option value="{{value}}" {{#selected}}selected="selected"{{/selected}}>{{name}}</option>
			{{/sortingOptions}}
		</select>
		{{/hasMaps}}
	</nav>
	<h2>{{messages.title}}</h2>
	{{#hasMaps}}
		<ul class="map-list">
			{{#maps}}
			<li>
				{{#done}}<a href="{{baseUrl}}/{{id}}">{{/done}}
				{{^done}}<div class="map-status">{{status_message}}</div>{{/done}}
				<img src="{{image}}" alt="{{title}}" height="{{map_height}}" width="{{map_width}}"/>
				<h3>{{title}}</h3>
				{{#done}}</a>{{/done}}
			</li>
			{{/maps}}
		</ul>
		{{{pagination}}}
	{{/hasMaps}}
	{{^hasMaps}}
	<h3>{{messages.no-maps-header}}</h3>
	<p>{{messages.no-maps-text}} <a href="{{learnMoreUrl}} title={{messages.no-maps-learn-more}}">
		{{messages.no-maps-learn-more}}</a></p>
	<button class="big" id="createMap">{{messages.create-a-map}}</button>
	{{/hasMaps}}
</section>
