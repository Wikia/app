{{#if standalone}}
	{{outlet}}
{{else}}
	<div class='main-container'>
		<header class='main-header'>Fandom Design System</header>
		{{main-navigation navigation=navigation}}
		<article class='main-content'>
			{{outlet}}
		</article>
	</div>
{{/if}}
