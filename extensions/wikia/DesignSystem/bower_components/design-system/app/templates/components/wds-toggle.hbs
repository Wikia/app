<input
	type="checkbox"
	id="{{id}}"
	class="wds-toggle__input"
	checked={{checked}}
	disabled={{disabled}}
	onChange={{action 'onChange'}}
>
<label for="{{id}}" class="wds-toggle__label">{{yield}}</label>
