<h2>{{topContribsHeaderText}}</h2>
<div class="user-details">
	<div class="avatar-container">
		{{{userAvatar}}}
	</div>
	<div class="rank">
		<span>{{userRank}} <small>/ {{weeklyEditorCount}}</small></span>
		<span class="subtle">{{yourRankText}}</span>
	</div>
	<div class="user-contrib-count">
		<span>{{userContribCount}}</span>
		<span class="subtle">{{userContributionsText}}</span>
	</div>
</div>
<ul class="reset top-contributors">
	{{#contributors}}
		<li>
			<div class="avatar-container">
				<a data-tracking="top-contributors-avatar" href="{{profilePage}}">{{{avatar}}}</a>
			</div>
			<div class="contributor-details">
				{{count}}. <a data-tracking="top-contributors" href="{{profilePage}}">{{userName}}</a>
				<span class="subtle">{{contributionsText}}</span>
			</div>

		</li>
	{{/contributors}}
	{{^contributors}}
		<div class="zero">
			{{noContribsText}}
		</div>
	{{/contributors}}
</ul>
