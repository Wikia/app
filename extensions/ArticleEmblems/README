# ArticleEmblems provides a parser hook that collects and renders extra information about an article

# This extension requires MediaWiki 1.17+ because it makes use of ResourceLoader.

# Example LocalSettings.php additions

require_once( "$IP/extensions/ArticleEmblems/ArticleEmblems.php" );

# To use this extension, add <emblem>any wiki-text you want</emblem> to an article. The wikitext will appear in the top
# right (or left when $wgContLang is an RTL language). Emblems will appear in the order they are found in the article.