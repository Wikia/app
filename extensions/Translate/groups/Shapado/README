# Read-only repository setup
cd %GROUPROOT%
git clone git://gitorious.org/shapado/shapado.git shapado

# Configuration
wfAddNamespace( 1226, 'Shapado' ); // Choose any free namespace number
$wgTranslateGroupFiles[] = "$IP/extensions/Translate/groups/Shapado/Shapado.yml";

# Commits
To do.
