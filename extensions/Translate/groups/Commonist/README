Check out read-only copy of the folder containing the L10n files:
cd $HOME/projects/
svn co http://svn.wikimedia.org/svnroot/mediawiki/trunk/tools/commonist/etc commonist

To export:
php export.php --target=$HOME/export --group=out-commonist --lang=*

Then copy, diff and commit.

TODO:
* cannot be supported until product supports UTF-8 in L10n files.
