---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: "mediawiki-cronjob-${name}"
spec:
  schedule: ${schedule}
  concurrencyPolicy: ${concurrencyPolicy}
  successfulJobsHistoryLimit: 0
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: php
            image: artifactory.wikia-inc.com/sus/mediawiki-sandbox-php:8e7b425.5c2dd23
            env:
             - name: SERVER_ID
               value: "${server_id}"
             - name: LOG_STDOUT_ONLY
               value: "yes"
            args:
${args}
          restartPolicy: Never

